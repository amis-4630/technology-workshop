/*
In JavaScript, objects and types are dynamic. You can add/remove properties to objects at any time.
*/

const user = {
    name: 'John',
    age: 22,
};

user.major = 'Computer Science';

console.log(user);