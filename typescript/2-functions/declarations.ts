/*
function declaration syntax is the most common way to define a function in TypeScript.
*/

function add(x: number, y: number): number {
    return x + y;
}

console.log(add(2, 2)); // 4

/*
The above code must be compiled for node to run it.
*/